# 06 - UART发送FIFO验证

## 📋 项目概述

**DUT**: UART TX with FIFO  
**协议**: AXI-Stream (输入) + UART (输出)  
**特性**: 16x8 FIFO, 流控, 可配置波特率

## 🎯 设计特点

### RTL设计 (208行)

#### uart_tx_fifo (69行)
- **容量**: 16字节
- **接口**: AXI-Stream
- **流控**: tvalid/tready握手
- **状态**: full, empty, count

#### uart_transmitter (89行)
- **波特率**: 115200 (可配置)
- **格式**: 8N1 (8位数据,无校验,1位停止)
- **状态机**: IDLE → START → DATA → STOP

#### uart_tx_top (50行)
- 集成FIFO + Transmitter
- 完整的发送路径

## ✅ AutoUVM生成的验证环境 (251行)

### Agent组件

1. **axis_driver** (42行)
   - AXI-Stream事务驱动
   - tvalid/tready握手
   - 可配置延迟

2. **uart_monitor** (53行)
   - UART协议监控
   - Start/Stop bit检测
   - 波特率自适应采样

3. **fifo_monitor** (38行)
   - FIFO状态监控
   - 深度统计
   - 满/空检测
   - 溢出告警

4. **uart_scoreboard** (68行)
   - AXIS输入 vs UART输出对比
   - 队列自动匹配
   - 完整统计报告

### 测试序列
- **uart_burst_seq** (31行)
  - Burst发送
  - 可配置间隔
  - 进度显示

## 📊 验证覆盖

- ✅ FIFO满/空状态
- ✅ 连续burst传输
- ✅ 背压(back-pressure)处理
- ✅ UART时序正确性
- ✅ 数据完整性

## 🔧 文件结构

```
06_uart_fifo/
├── rtl/
│   ├── uart_tx_fifo.v       (69行)  FIFO
│   ├── uart_transmitter.v   (89行)  发送器
│   └── uart_tx_top.v        (50行)  顶层
├── generated_uvm/
│   ├── agents/
│   │   ├── axis_driver.sv
│   │   ├── uart_monitor.sv
│   │   ├── fifo_monitor.sv
│   │   └── uart_scoreboard.sv
│   └── sequences/
│       └── uart_burst_seq.sv
└── README.md
```

## 💡 关键验证点

1. **FIFO功能**: 读写指针、计数器正确
2. **流控**: tready在FIFO满时拉低
3. **UART时序**: Start/Data/Stop bit时序符合标准
4. **数据完整性**: 输入数据==输出数据
5. **性能**: FIFO深度利用率统计

## 🚀 特色

- AXI-Stream标准接口
- 完整的FIFO状态监控
- 波特率自适应采样
- 实时深度统计
- 背压场景测试

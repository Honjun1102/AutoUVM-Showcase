# 测试套件说明

## 📋 测试用例 (6个)

### 1. concurrent_access_test (并发访问测试)
**目的**: 验证多个Master同时访问不同Slave的基本功能
- **场景**: 3个Master (CPU/DMA/Debug) 并发发起事务
- **验证点**: 
  - 基本互联功能
  - TLM通信正确性
  - Scoreboard事务匹配
- **运行时间**: ~1μs

### 2. stress_test (压力测试)
**目的**: 测试系统在高负载下的稳定性
- **场景**: CPU和DMA各发200个burst事务
- **验证点**:
  - 高负载下的数据完整性
  - Crossbar缓冲区不溢出
  - 无死锁
- **运行时间**: ~2μs

### 3. outstanding_test (Outstanding事务测试)
**目的**: 验证Outstanding事务处理
- **场景**: 同时发送8个outstanding事务，10轮
- **验证点**:
  - Outstanding事务正确排序
  - ID匹配正确
  - 响应顺序验证
- **运行时间**: ~1μs

### 4. arbiter_test (仲裁测试)
**目的**: 验证仲裁器的公平性和优先级
- **场景**: 3个Master同时竞争相同/不同Slave
- **验证点**:
  - Round-robin仲裁
  - 无饥饿现象
  - 仲裁延迟统计
- **运行时间**: ~1μs

### 5. address_decode_test (地址解码测试)
**目的**: 验证地址解码和路由正确性
- **场景**: 扫描所有Slave的地址空间
- **验证点**:
  - 地址范围正确路由
  - 边界地址处理
  - 无效地址响应
- **运行时间**: ~1μs

### 6. random_traffic_test (随机流量测试)
**目的**: 模拟真实系统随机流量
- **场景**: 随机延迟、随机地址、随机burst长度
- **验证点**:
  - 随机场景下功能正确
  - 边界条件覆盖
  - 统计分析
- **运行时间**: ~2μs

---

## 🧩 测试序列 (8个)

### 基础序列

#### 1. cpu_random_seq
- 完全随机的CPU访问
- 短burst (0-3 beats)
- 随机间隔 (10-50ns)

#### 2. dma_burst_seq
- DMA长burst传输
- 长burst (4-15 beats)
- INCR类型

#### 3. debug_reg_seq
- Debug寄存器访问
- 单次传输
- 访问Timer (通过Bridge)

### 压力序列

#### 4. stress_burst_seq
- 大量burst事务 (100+)
- 长burst (8-15 beats)
- 持续高负载

#### 5. back2back_seq
- 背靠背传输
- 无间隔延迟
- 连续地址访问

#### 6. random_delay_seq
- 随机延迟
- 可配置延迟范围
- 模拟真实流量

### 高级序列

#### 7. outstanding_seq
- 多个outstanding事务
- 可配置outstanding数量
- 短间隔发送

#### 8. address_sweep_seq
- 地址空间扫描
- 可配置起止地址和步长
- 用于地址解码验证

---

## 🚀 运行方法

### 运行单个测试
```bash
make sim TEST=stress_test
make sim TEST=outstanding_test VERB=UVM_HIGH
```

### 运行所有测试
```bash
make run_all
```

### 生成覆盖率报告
```bash
make coverage
```

---

## 📊 测试覆盖矩阵

| 测试用例 | Crossbar | Bridge | Arbiter | Outstanding | 地址解码 |
|---------|----------|--------|---------|-------------|----------|
| concurrent_access_test | ✓ | ✓ | ✓ | - | ✓ |
| stress_test | ✓✓ | ✓ | ✓✓ | ✓ | - |
| outstanding_test | ✓ | - | ✓ | ✓✓✓ | - |
| arbiter_test | ✓ | - | ✓✓✓ | - | - |
| address_decode_test | ✓ | ✓ | - | - | ✓✓✓ |
| random_traffic_test | ✓✓ | ✓✓ | ✓✓ | ✓ | ✓ |

图例: ✓ 基础覆盖, ✓✓ 重点覆盖, ✓✓✓ 深度覆盖

---

## 📈 预期结果

所有测试应该:
- ✅ 无UVM_ERROR
- ✅ 无UVM_FATAL
- ✅ Scoreboard无mismatch
- ✅ 所有事务正确完成
- ⚠️ 可能有UVM_WARNING (性能相关)

---

## 🔧 定制测试

可以通过修改序列参数来定制测试:
```systemverilog
stress_seq.num_trans = 500;  // 增加事务数
outstanding_seq.num_outstanding = 16;  // 增加outstanding
random_seq.min_delay = 0;  // 减少延迟
```
